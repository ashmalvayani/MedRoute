# ChestXray8

NIH ChestX-ray8 dataset. Split-based CSV layout.

Download the dataset and place the splits as:

- `data/dev/*.csv`
- `data/test/*.csv`
- `data/image_embeddings/` — optional pre-computed embeddings (`.pt` files; ignored by git)

## Download

<add download URL / HF dataset ID here>

## Expected CSV format

<add column names and a one-row example here>
