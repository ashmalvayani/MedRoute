# PathVQA

Pathology visual question answering. Single-file JSON layout.

Download the dataset and place:

- `data/pathvqa.json`        — list of records
- `data/image_embeddings/`   — optional pre-computed embeddings (`.pt` files; ignored by git)

## Download

<add download URL / HF dataset ID here>
